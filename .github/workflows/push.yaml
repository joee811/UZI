name: test
on: [push]
jobs:
  job-thing:
    if: ${{ github.ref == 'refs/heads/main' }}
    runs-on: ubuntu-latest
    steps:
      - name: dump-job
        env:
          JOB_CONTEXT: ${{ job.status }}
        run: echo "$JOB_CONTEXT"
